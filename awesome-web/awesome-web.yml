# Copyright 2025 Luka Kladarić, Chaos Guru
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

AWSTemplateFormatVersion: '2010-09-09'
Description: Awesome Foundation Web v0.2

Parameters:
  AdditionalSSLCertificate:
    Type: String
    Description: ARN of an additional SSL certificate to attach to the load balancers (for example, to cover additional domains)
    Default: ''
  # AZ deployment parameters - must match VPC stack settings
  # Using 0/1 instead of yes/no due to rain/CloudFormation boolean vs string collision
  DeployAZOne:
    Type: String
    Description: Whether to deploy resources in Availability Zone One (1=yes, 0=no)
    AllowedValues:
      - 1
      - 0
    Default: 1
  DeployAZTwo:
    Type: String
    Description: Whether to deploy resources in Availability Zone Two (1=yes, 0=no)
    AllowedValues:
      - 1
      - 0
    Default: 1
  DeployAZThree:
    Type: String
    Description: Whether to deploy resources in Availability Zone Three (1=yes, 0=no)
    AllowedValues:
      - 1
      - 0
    Default: 0

Mappings:
  RegionalConfigs:
    # From https://docs.aws.amazon.com/elasticloadbalancing/latest/application/enable-access-logging.html
    us-east-1:
      ELBAccountId: "127311923021"
    us-east-2:
      ELBAccountId: "033677994240"
    us-west-1:
      ELBAccountId: "027434742980"
    us-west-2:
      ELBAccountId: "797873946194"
    af-south-1:
      ELBAccountId: "098369216593"
    ap-east-1:
      ELBAccountId: "754344448648"
    ap-southeast-3:
      ELBAccountId: "589379963580"
    ap-south-1:
      ELBAccountId: "718504428378"
    ap-northeast-3:
      ELBAccountId: "383597477331"
    ap-northeast-2:
      ELBAccountId: "600734575887"
    ap-southeast-1:
      ELBAccountId: "114774131450"
    ap-southeast-2:
      ELBAccountId: "783225319266"
    ap-northeast-1:
      ELBAccountId: "582318560864"
    ca-central-1:
      ELBAccountId: "985666609251"
    eu-central-1:
      ELBAccountId: "054676820928"
    eu-west-1:
      ELBAccountId: "156460612806"
    eu-west-2:
      ELBAccountId: "652711504416"
    eu-south-1:
      ELBAccountId: "635631232127"
    eu-west-3:
      ELBAccountId: "009996457667"
    eu-north-1:
      ELBAccountId: "897822967062"
    me-south-1:
      ELBAccountId: "076674570225"
    sa-east-1:
      ELBAccountId: "507241528517"
    us-gov-east-1:
      ELBAccountId: "190560391635"
    us-gov-west-1:
      ELBAccountId: "048591011584"

Conditions:
  AdditionalSSLCertProvided: !Not [!Equals [!Ref AdditionalSSLCertificate, '']]
  DeployAZOne: !Equals [!Ref DeployAZOne, 1]
  DeployAZTwo: !Equals [!Ref DeployAZTwo, 1]
  DeployAZThree: !Equals [!Ref DeployAZThree, 1]

Resources:
  # Create an SSL certificate for *.Stage.BaseDomain, use DNS validation
  SSLCertificate:
    Type: AWS::CertificateManager::Certificate
    Properties:
      DomainName:
        !Sub
          - '*.${DNSZoneName}'
          - DNSZoneName: !ImportValue 'awesome-vpc:DNSZoneName'
      ValidationMethod: DNS
      DomainValidationOptions:
        - DomainName:
            !Sub
              - '*.${DNSZoneName}'
              - DNSZoneName: !ImportValue 'awesome-vpc:DNSZoneName'
          HostedZoneId: !ImportValue 'awesome-vpc:DNSZoneId'

  ## BEGIN load balancing
  # Create an S3 bucket to store the ALB logs in
  ALBLogs: # Giving this a short name because the key becomes part of the unique bucket name
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: 'AES256'
      PublicAccessBlockConfiguration:
        BlockPublicAcls: True
        BlockPublicPolicy: True
        IgnorePublicAcls: True
        RestrictPublicBuckets: True

  ALBLogsBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref ALBLogs
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: AWSALBLogDeliveryWrite
            Effect: Allow
            Principal:
              AWS: !Sub
                - 'arn:aws:iam::${ELBAccountId}:root'
                - ELBAccountId: !FindInMap [RegionalConfigs, !Ref 'AWS::Region', ELBAccountId]
            Resource:
              - !Sub 'arn:aws:s3:::${ALBLogs}/AWSLogs/${AWS::AccountId}/*'
            Action: s3:PutObject
          - Sid: AWSLogDeliveryWrite
            Effect: Allow
            Principal:
              Service: delivery.logs.amazonaws.com
            Resource:
              - !Sub 'arn:aws:s3:::${ALBLogs}/AWSLogs/${AWS::AccountId}/*'
            Action: s3:PutObject
            Condition:
              StringEquals:
                s3:x-amz-acl: bucket-owner-full-control
          - Sid: AWSLogDeliveryAclCheck
            Effect: Allow
            Principal:
              Service: delivery.logs.amazonaws.com
            Action: s3:GetBucketAcl
            Resource:
              - !Sub 'arn:aws:s3:::${ALBLogs}'

  # A public facing load balancer, this is used for accepting traffic from the public
  # internet and directing it to public facing services
  LoadBalancerPublic:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Scheme: internet-facing
      LoadBalancerAttributes:
      - Key: idle_timeout.timeout_seconds
        Value: '60'
      - Key: routing.http.drop_invalid_header_fields.enabled
        Value: true
      - Key: access_logs.s3.enabled
        Value: "true"
      - Key: access_logs.s3.bucket
        Value: !Ref ALBLogs
      Subnets:
        # The load balancer is placed into the public subnets, so that traffic
        # from the internet can reach the load balancer directly via the internet gateway
        # Note: ALBs require at least 2 subnets in different AZs
        - !If [DeployAZOne, !ImportValue 'awesome-vpc:SubnetPublicOne', !Ref 'AWS::NoValue']
        - !If [DeployAZTwo, !ImportValue 'awesome-vpc:SubnetPublicTwo', !Ref 'AWS::NoValue']
        - !If [DeployAZThree, !ImportValue 'awesome-vpc:SubnetPublicThree', !Ref 'AWS::NoValue']
      SecurityGroups:
        - !ImportValue 'awesome-vpc:SecurityGroupVeryPermissive'

  # A dummy target group is used to setup the ALB to just drop traffic
  # initially, before any real service target groups have been added.
  TargetGroupDummyPublic:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      HealthCheckIntervalSeconds: 6
      HealthCheckPath: /
      HealthCheckProtocol: HTTP
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      Name: !Sub '${AWS::StackName}-drop-public'
      Port: 80
      Protocol: HTTP
      UnhealthyThresholdCount: 2
      VpcId: !ImportValue 'awesome-vpc:VPCId'

  # This is the Public LB HTTPS listener which forwards traffic to the dummy target group
  LoadBalancerListenerPublic:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      DefaultActions:
        - TargetGroupArn: !Ref 'TargetGroupDummyPublic'
          Type: 'forward'
      LoadBalancerArn: !Ref 'LoadBalancerPublic'
      Port: 443
      Protocol: HTTPS
      Certificates:
        - CertificateArn: !Ref 'SSLCertificate'
      SslPolicy: 'ELBSecurityPolicy-TLS13-1-2-2021-06'

  # The default certificate covers only the base domain, this one covers additional domains
  LoadBalancerListenerPublicAdditionalCertificate:
    Type: AWS::ElasticLoadBalancingV2::ListenerCertificate
    Condition: AdditionalSSLCertProvided
    Properties:
      Certificates:
        - CertificateArn: !Ref 'AdditionalSSLCertificate'
      ListenerArn: !Ref LoadBalancerListenerPublic

  # This is the Public LB HTTP listener which auto-upgrades any request to HTTPS
  LoadBalancerListenerHTTPSUpgradePublic:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      DefaultActions:
        - RedirectConfig:
            StatusCode: HTTP_301
            Protocol: HTTPS
            Port: '443'
            Host: '#{host}'
            Path: '/#{path}'
            Query: '#{query}'
          Type: 'redirect'
      LoadBalancerArn: !Ref 'LoadBalancerPublic'
      Port: 80
      Protocol: HTTP
    Metadata:
      cfn_nag:
        rules_to_suppress:
          - id: W56
            reason: "This is the HTTP to HTTPS upgrade listener, it's supposed to be HTTP."

  # The private facing load balancer, this is used for accepting traffic from internal clients
  LoadBalancerPrivate:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Scheme: internal
      LoadBalancerAttributes:
      - Key: idle_timeout.timeout_seconds
        Value: '60'
      - Key: routing.http.drop_invalid_header_fields.enabled
        Value: true
      - Key: access_logs.s3.enabled
        Value: "true"
      - Key: access_logs.s3.bucket
        Value: !Ref ALBLogs
      Subnets:
        # This load balancer is put into the private subnet, so that there is no
        # route for the public to even be able to access the private load balancer.
        # Note: ALBs require at least 2 subnets in different AZs
        - !If [DeployAZOne, !ImportValue 'awesome-vpc:SubnetPrivateOne', !Ref 'AWS::NoValue']
        - !If [DeployAZTwo, !ImportValue 'awesome-vpc:SubnetPrivateTwo', !Ref 'AWS::NoValue']
        - !If [DeployAZThree, !ImportValue 'awesome-vpc:SubnetPrivateThree', !Ref 'AWS::NoValue']
      SecurityGroups:
        - !ImportValue 'awesome-vpc:SecurityGroupVeryPermissive'

  # This dummy target group is used to setup the ALB to just drop traffic
  # initially, before any real service target groups have been added.
  TargetGroupDummyPrivate:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      HealthCheckIntervalSeconds: 6
      HealthCheckPath: /
      HealthCheckProtocol: HTTP
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      Name: !Sub '${AWS::StackName}-drop-private'
      Port: 80
      Protocol: HTTP
      UnhealthyThresholdCount: 2
      VpcId: !ImportValue 'awesome-vpc:VPCId'

  # This is the Private LB HTTPS listener which forwards traffic to the dummy target group
  LoadBalancerListenerPrivate:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      DefaultActions:
        - TargetGroupArn: !Ref 'TargetGroupDummyPrivate'
          Type: 'forward'
      LoadBalancerArn: !Ref 'LoadBalancerPrivate'
      Port: 443
      Protocol: HTTPS
      Certificates:
        - CertificateArn: !Ref 'SSLCertificate'
      SslPolicy: 'ELBSecurityPolicy-TLS13-1-2-2021-06'

  # The default certificate covers only the base domain, this one covers additional domains
  LoadBalancerListenerPrivateAdditionalCertificate:
    Type: AWS::ElasticLoadBalancingV2::ListenerCertificate
    Condition: AdditionalSSLCertProvided
    Properties:
      Certificates:
        - CertificateArn: !Ref 'AdditionalSSLCertificate'
      ListenerArn: !Ref LoadBalancerListenerPrivate

  # This is the Private LB HTTP listener which auto-upgrades any request to HTTPS
  LoadBalancerListenerHTTPSUpgradePrivate:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      DefaultActions:
        - RedirectConfig:
            StatusCode: HTTP_301
            Protocol: HTTPS
            Port: '443'
            Host: '#{host}'
            Path: '/#{path}'
            Query: '#{query}'
          Type: 'redirect'
      LoadBalancerArn: !Ref 'LoadBalancerPrivate'
      Port: 80
      Protocol: HTTP
    Metadata:
      cfn_nag:
        rules_to_suppress:
          - id: W56
            reason: "This is the HTTP to HTTPS upgrade listener, it's supposed to be HTTP."
  ## END load balancing

  ## BEGIN ALB rule priority assignment lambda
  # lifted from https://stackoverflow.com/questions/50003378/automatically-set-listenerrule-priority-in-cloudformation-template
  LambdaRoleALBRulePriorityFixer:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: ""
            Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      Path: /
      Policies:
        - PolicyName: DescribeRulesPolicy
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - elasticloadbalancing:DescribeRules
                Resource: '*'
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
    Metadata:
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Rule Priority Lambda needs this policy to do its thing."

  LambdaALBRulePriorityFixerPublic:
    Type: AWS::Lambda::Function
    Properties:
      Handler: index.lambda_handler
      Role: !GetAtt LambdaRoleALBRulePriorityFixer.Arn
      Code:
        ZipFile: !Rain::Embed allocate_alb_rule_priority.py
      Runtime: python3.8
      Timeout: "30"
      Environment:
        Variables:
          ListenerArn: !Ref LoadBalancerListenerPublic
    Metadata:
      cfn_nag:
        rules_to_suppress:
          - id: W89
            reason: "Lambdas definitely should not be deployed in a VPC."

  LambdaALBRulePriorityFixerPrivate:
    Type: AWS::Lambda::Function
    Properties:
      Handler: index.lambda_handler
      Role: !GetAtt LambdaRoleALBRulePriorityFixer.Arn
      Code:
        ZipFile: !Rain::Embed allocate_alb_rule_priority.py
      Runtime: python3.8
      Timeout: "30"
      Environment:
        Variables:
          ListenerArn: !Ref LoadBalancerListenerPrivate
    Metadata:
      cfn_nag:
        rules_to_suppress:
          - id: W89
            reason: "Lambdas definitely should not be deployed in a VPC."
  ## END ALB rule priority assignment lambda

  ## BEGIN DNS records
  DNSRecordLoadBalancerPublic:
    Type: AWS::Route53::RecordSet
    Properties:
      Name: !Sub
        - 'lb-public.${DNSZoneName}'
        - DNSZoneName: !ImportValue 'awesome-vpc:DNSZoneName'
      HostedZoneId: !ImportValue 'awesome-vpc:DNSZoneId'
      Type: CNAME
      TTL: 300
      ResourceRecords:
        - !GetAtt 'LoadBalancerPublic.DNSName'

  DNSRecordLoadBalancerPrivate:
    Type: AWS::Route53::RecordSet
    Properties:
      Name: !Sub
        - 'lb-private.${DNSZoneName}'
        - DNSZoneName: !ImportValue 'awesome-vpc:DNSZoneName'
      HostedZoneId: !ImportValue 'awesome-vpc:DNSZoneId'
      Type: CNAME
      TTL: 300
      ResourceRecords:
        - !GetAtt 'LoadBalancerPrivate.DNSName'

  ## END DNS records

  # ECS Cluster
  # Calling it "default" simplifies addressing it from CLI and API.
  DefaultECSCluster:
    DependsOn: ECSRole
    Type: AWS::ECS::Cluster
    Properties:
      ClusterName: default
      CapacityProviders:
        - FARGATE
        - FARGATE_SPOT
      ClusterSettings:
        - Name: containerInsights
          Value: enhanced # enhanced container insights must be enabled at account level :rolleyes:

  # This is an IAM role which authorizes ECS to manage resources on your
  # account on your behalf, such as updating your load balancer with the
  # details of where your containers are, so that traffic can reach your
  # containers.
  ECSRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Statement:
        - Effect: Allow
          Principal:
            Service: [ecs.amazonaws.com]
          Action: ['sts:AssumeRole']
      Path: /
      Policies:
      - PolicyName: ecs-service
        PolicyDocument:
          Statement:
          - Effect: Allow
            Action:
              # Rules which allow ECS to attach network interfaces to instances
              # on your behalf in order for awsvpc networking mode to work right
              - 'ec2:AttachNetworkInterface'
              - 'ec2:CreateNetworkInterface'
              - 'ec2:CreateNetworkInterfacePermission'
              - 'ec2:DeleteNetworkInterface'
              - 'ec2:DeleteNetworkInterfacePermission'
              - 'ec2:Describe*'
              - 'ec2:DetachNetworkInterface'

              # Rules which allow ECS to update load balancers on your behalf
              # with the information sabout how to send traffic to your containers
              - 'elasticloadbalancing:DeregisterInstancesFromLoadBalancer'
              - 'elasticloadbalancing:DeregisterTargets'
              - 'elasticloadbalancing:Describe*'
              - 'elasticloadbalancing:RegisterInstancesWithLoadBalancer'
              - 'elasticloadbalancing:RegisterTargets'
            Resource: '*'
    Metadata:
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "ECS needs this policy to do its thing."

  # This is a role which is used to launch ECS tasks
  ECSTaskExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Statement:
        - Effect: Allow
          Principal:
            Service: [ecs-tasks.amazonaws.com]
          Action: ['sts:AssumeRole']
      Path: /
      Policies:
        - PolicyName: AmazonECSTaskExecutionRolePolicy
          PolicyDocument:
            Statement:
            - Effect: Allow
              Action:
                # Allow the ECS Tasks to download images from ECR
                - 'ecr:GetAuthorizationToken'
                - 'ecr:BatchCheckLayerAvailability'
                - 'ecr:GetDownloadUrlForLayer'
                - 'ecr:BatchGetImage'

                # Allow the ECS tasks to upload logs to CloudWatch
                - 'logs:CreateLogGroup' # ideally log groups will be created explicitly instead of implicitly but it's an imperfect world
                - 'logs:CreateLogStream'
                - 'logs:PutLogEvents'
              Resource: '*'
    Metadata:
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "ECS needs this policy to do its thing."

# These are the values output by the CloudFormation template. Be careful
# about changing any of them, because of them are exported with specific
# names so that the other task related CF templates can use them.
Outputs:
  ClusterName:
    Description: The name of the ECS cluster
    Value: !Ref 'DefaultECSCluster'
    Export:
      Name: !Sub '${AWS::StackName}:ClusterName'
  HostnamePrivate:
    Description: The url of the internal load balancer
    Value: !Ref DNSRecordLoadBalancerPrivate
    Export:
      Name: !Sub '${AWS::StackName}:HostnamePrivate'
  HostnamePublic:
    Description: The url of the external load balancer
    Value: !Ref DNSRecordLoadBalancerPublic
    Export:
      Name: !Sub '${AWS::StackName}:HostnamePublic'
  ECSRole:
    Description: The ARN of the ECS role
    Value: !GetAtt 'ECSRole.Arn'
    Export:
      Name: !Sub '${AWS::StackName}:ECSRole'
  ECSTaskExecutionRole:
    Description: The ARN of the ECS role
    Value: !GetAtt 'ECSTaskExecutionRole.Arn'
    Export:
      Name: !Sub '${AWS::StackName}:ECSTaskExecutionRole'
  ListenerPublic:
    Description: The ARN of the public load balancer's Listener
    Value: !Ref LoadBalancerListenerPublic
    Export:
      Name: !Sub '${AWS::StackName}:ListenerPublic'
  ALBLogsBucket:
    Description: The bucket for ALB logs in this account
    Value: !Ref ALBLogs
    Export:
      Name: !Sub '${AWS::StackName}:ALBLogsBucket'
  SSLCertificateArn:
    Description: The ARN of the SSL certificate
    Value: !Ref SSLCertificate
    Export:
      Name: !Sub '${AWS::StackName}:SSLCertificateArn'
  ListenerPrivate:
    Description: The ARN of the public load balancer's Listener
    Value: !Ref LoadBalancerListenerPrivate
    Export:
      Name: !Sub '${AWS::StackName}:ListenerPrivate'
  LambdaALBRulePriorityFixerPublic:
    Value: !GetAtt LambdaALBRulePriorityFixerPublic.Arn
    Export:
      Name: !Sub '${AWS::StackName}:LambdaALBRulePriorityFixerPublic'
  LambdaALBRulePriorityFixerPrivate:
    Value: !GetAtt LambdaALBRulePriorityFixerPrivate.Arn
    Export:
      Name: !Sub '${AWS::StackName}:LambdaALBRulePriorityFixerPrivate'
